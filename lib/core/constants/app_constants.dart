class AppConstants {
  AppConstants._();

  static const appName = 'Books Discovery';

  static const debounceDuration = Duration(milliseconds: 500);

  static const pageSize = 20;

  static const animationDuration = Duration(milliseconds: 300);
}
